include<stdio.h>
include<math.h>

double f(double e,double T,double del)
{
    double f;
    f=tanh(0.5*sqrt(e*e+del*del)/T)/(sqrt(e*e+del*del));
    return f;
}
int main()
{
    int i;
    double s;
ff

}